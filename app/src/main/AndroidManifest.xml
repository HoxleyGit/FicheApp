<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.fiche.ficheapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CurrentLessonActivity" />
        <activity android:name=".NewEditFicheActivity" />
        <activity android:name=".ExamQuestionActivity" />
        <activity android:name=".ExamAnswerActivity" />
        <activity android:name=".ExamResultActivity" />


        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AllCategoriesActivity" />
    </application>

</manifest>